const { resolve } = require('path');

module.exports = {
  config: resolve(__dirname, 'src', 'config', 'database.js'),
  'models-path': resolve(__dirname, 'src', 'models'),
  'migrations-path': resolve(__dirname, 'src', 'database', 'migrations'), // gerencia mudanças na db como criação de tabelas e registra isso
  'seeders-path': resolve(__dirname, 'src', 'database', 'seeds'), // msm
};
